# Pending tasks

Tasks intentionally deferred ("to do later") or descoped on request - paused with a
recorded reason, NOT abandoned. Distinct from Blocked (short-term wait). Moved back to
active/ when resumed. The orchestrator's session-start scan skips this folder; resume
explicitly.
