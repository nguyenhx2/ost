# Known issues - OST

| Vấn đề | Workaround | Trạng thái | Phát hiện |
|--------|-----------|------------|-----------|
| Rust toolchain chưa cài trên máy dev (cargo/rustc not found) | Đã cài rustup 1.96.1 (TASK-001) | Resolved 2026-07-09 | 2026-07-09 |
| rustc không tự tìm được MSVC link.exe từ Git Bash: GNU `link` (coreutils) che PATH; PATH sạch thì vswhere-detect cũng fail với VS 18 Enterprise | Mọi lệnh cargo trong agent shell bọc qua vcvars64: `cmd //c "\"C:\Program Files\Microsoft Visual Studio\18\Enterprise\VC\Auxiliary\Build\vcvars64.bat\" >nul 2>&1 && set PATH=%USERPROFILE%\.cargo\bin;%PATH% && cargo <args>"` | Open (workaround ổn định) | 2026-07-09 |
| `~/.cargo/bin` không có trong PATH của Git Bash session | `export PATH="$USERPROFILE/.cargo/bin:$PATH"` hoặc dùng cmd wrapper ở trên | Open | 2026-07-09 |
| Wrapper vcvars một dòng (`cmd //c "\"...\""`) bị Git Bash phá escape quotes khi chạy từ agent shell | Tạo file `.bat` wrapper (call vcvars64 >nul, prepend `%USERPROFILE%\.cargo\bin`, `cd /d <dir>`, `cargo %*`) rồi gọi `cmd //c <bat> <args>`; background bash không kế thừa repo cwd - luôn `cd /d` tuyệt đối | Open (workaround ổn định) | 2026-07-09 |
| Windows SDK thiếu hoàn toàn trên máy dev (link fail LNK1181 kernel32.lib) dù VS 18 có VC Tools | ĐÃ CÀI Windows 11 SDK 10.0.26100.7705 qua winget (Microsoft.WindowsSDK.10.0.26100) trong TASK-002 | Resolved 2026-07-09 | 2026-07-09 |
| Compile Rust song song có thể lỗi OS 1455 (paging file too small) trên máy này | Chạy lại với `cargo <cmd> -j 2` | Open | 2026-07-09 |
| Sandbox của agent Bash tool chặn kết nối loopback: vite bind được :1420 nhưng mọi kết nối kẹt SYN_RECEIVED (kể cả probe của tauri dev cùng job) | Chạy `npm run tauri dev`/dev server với sandbox tắt (dangerouslyDisableSandbox) hoặc user tự chạy | Open (workaround ổn định) | 2026-07-09 |
| Prettier fail toàn bộ trên GitHub windows runner do autocrlf=true đổi LF->CRLF khi checkout | Đã thêm `.gitattributes` (`* text=auto eol=lf`, bat/ps1 giữ CRLF) | Resolved 2026-07-09 | 2026-07-09 |
| tauri-driver (e2e) còn hạn chế trên Windows - đánh giá lại khi wire e2e | Đã wire e2e (TASK-022, tauri-driver 2.0.6 + msedgedriver khớp WebView2). Chi tiết + phần chia CI/dev-host/manual ở hàng bên dưới | Resolved 2026-07-11 | 2026-07-09 |
| e2e (TASK-022) - các ràng buộc thực tế của tauri-driver + WebView2 trên máy này: (1) chỉ chạy được với RELEASE binary (debug/`tauri dev` load localhost:1420 bị chặn; chỉ release nhúng frontend nên session mới tới được `http://tauri.localhost`); (2) msedgedriver PHẢI khớp WebView2 runtime (hiện 150.0.4078.48) - pin dưới `e2e/.driver/` (gitignored); (3) tauri-driver chỉ gắn vào MỘT WebView (cửa sổ main) - không chuyển sang được cửa sổ preview/settings tạo động, nên các view được lái bằng cách điều hướng `?view=` trên chính WebView đó; (4) sự kiện `region:ocr-result/error` emit tới cửa sổ preview mà driver KHÔNG quan sát được, nên đường region được lái + quan sát qua command `e2e_region_probe` (chỉ có dưới feature `e2e`, không có trong bản production) trả về kết quả trực tiếp; (5) tạo session đôi khi ETIMEDOUT lần đầu - kill `ost/tauri-driver/msedgedriver` còn sót rồi chạy lại | Build release qua `npm run tauri build -- --no-bundle` (bọc vcvars); `npm run test:e2e`. `region-select` cần DISPLAY thật + OCR models có sẵn (`~/.oar`) để leg capture thật chạy - đây là DEV-HOST, không phải CI headless. `smoke`/`settings`/`overlay` chạy được ở bất kỳ host có display + msedgedriver khớp (ứng viên CI), NHƯNG xanh KHÔNG chứng minh capture chạy | Open | 2026-07-11 |
| Branch protection cho `main` không bật được: repo private trên GitHub Free (403 - cần Pro hoặc repo public). Owner quyết định defer (2026-07-09) | ĐÃ BẬT 2026-07-11: owner chuyển repo sang PUBLIC (sau full-history secret scan sạch), branch protection main yêu cầu check `lint-and-test` + strict, cấm force-push/delete | Resolved 2026-07-11 | 2026-07-09 |
| Escalation ladder R2 của ADR-004: promote Windows.Media.Ocr làm OCR default khi local spike (R1) fail sẽ hy sinh Vietnamese coverage (WMO không có tiếng Việt trong ~25 ngôn ngữ OCR) và không có per-line confidence | Owner đã đặt Vietnamese coverage là yêu cầu bắt buộc (2026-07-09): WMO không còn là fallback tự do - chỉ được promote làm default dưới một amendment AC-02.6 do owner ký (R2 ladder bước 4); ưu tiên R2 bước 1-2 (PP-OCRv6 tiny, downscale/skip det cho subtitle một dòng) trước khi tính tới WMO | Open | 2026-07-09 |
| OCR tiếng Việt bị charset-cap ~0.74 trên PP-OCRv5 mobile (oar-ocr 0.8.0): rec dict thiếu dải diacritic tổ hợp U+1E00-U+1EFF nên dấu thanh bị rơi; lỗi rơi dấu mang confidence cao (~0.970) nên KHÔNG bị cờ low-confidence bắt (AC-02.6/BR-05) | Bắt buộc khai báo độ trung thực `Degraded{reason}` cho vi + notice cố định trong preview (amendment AC-02.6/OI-07 do owner ký 2026-07-10); remedy thật là một rec model vi-finetuned (ADR-004 option b); revisit khi tiếng Việt trở thành ngôn ngữ nguồn chính | Open | 2026-07-10 |
| Cloud OCR opt-in (BR-09) đã được owner cho phép về nguyên tắc nhưng KHÔNG triển khai được trong ngân sách hiện tại: FR-02 bắt region p95 < 2s (AC-02.2, NFR-PERF-02, BR-04), OCR local đã tốn 277ms, cloud OCR chèn thêm một round-trip mạng NỐI TIẾP trước cả cuộc gọi dịch LLM | Owner quyết định 2026-07-10: park TASK-011 tới sau MVP, KHÔNG nới ngân sách, KHÔNG mở egress path. Đường OCR local đã đạt EN/JA 1.000 và không nằm trên đường găng. Unpark khi: có nhu cầu thật, HOẶC spike đo được chuỗi cloud-OCR + dịch lọt 2s, HOẶC owner ký nới AC-02.2 cho riêng đường opt-in | Deferred | 2026-07-10 |
| Debug builds and `tauri dev` load the WebView from `http://localhost:1420`; this environment blocks loopback so those are unusable. Additionally the region pipeline was debug-MOCKED, hiding the real capture path until release (TASK-021 hang). | VERIFY on the RELEASE binary: `npm run tauri build -- --no-bundle` (embedded `../dist`, loaded via `tauri://`) then run `ost.exe`. Never rely on debug/`tauri dev` for capture/region behaviour. | Open | 2026-07-11 |
| GitHub Actions hết hạn mức free trên repo private (run trên main fail sau 3s, 2026-07-11); cổng merge "CI xanh" tạm thay bằng bộ verify local ghi vào task file + PR body | ĐÃ GIẢI QUYẾT 2026-07-11: owner chuyển repo sang public (Actions miễn phí không giới hạn cho public repo) sau khi full-history secret scan sạch (không key/env/PII trong mọi revision). Cổng CI khôi phục bình thường; bỏ chế độ verify-local-thay-CI | Resolved 2026-07-11 | 2026-07-11 |
| Cloud STT (ADR-005) đã được nghiên cứu đầy đủ nhưng gửi AUDIO THÔ rời máy - thay đổi privacy nặng nhất (nặng hơn cloud OCR); chính nghiên cứu kết luận cloud STT hiện không đủ hấp dẫn để đánh đổi egress khi whisper local với switcher tier (tiny/base/small/large-v3-turbo) đã phủ nhu cầu ja/en | Owner quyết định 2026-07-11: DEFER như TASK-011 - ADR-005 giữ Proposed, KHÔNG ký, không amendment BR-01/NFR-SEC-03, không mở đường audio rời máy. UI giữ 3 dòng cloud disabled ghi "pending ADR-005". Unpark khi: có nhu cầu thật (máy yếu không chạy nổi whisper local), HOẶC owner ký ADR-005 + amendments | Deferred | 2026-07-11 |
| Flaky provider tests under a full local parallel `cargo test` | ~20 tests in `providers::anthropic`/`providers::gemini`/`providers::local_openai` intermittently fail with wiremock `Timeout { timeout_ms: 30000 }` / wrong-`ProviderError`-variant assertions when the whole suite runs in parallel on a dev host. They pass 123/123 when re-run in isolation (`cargo test --lib providers:: -- --test-threads=1`), and CI is green. Suspected wiremock server/port contention, not a logic defect. Needs triage; no owner yet. | Open | 2026-07-11 |
| Follow-ups from TASK-027/028/029 reviews | Non-blocking follow-up items: (1) `Badge label=` overrides child content, so assistive tech announces only the label and never the value (provider/model, hotkey) - pattern used in `SettingsView`, `RegionPreviewView`, home screen - worth fixing at `Badge` primitive / call-site level project-wide. (2) Five `open_*_window` functions still return `Result<(), _>` that can now never be `Err` (build deferred), leaving unreachable error branches in `shell/hotkeys.rs` and `shell/tray.rs` - consider narrowing signatures or documenting why `Result` retained. (3) No test covers two concurrent first-time opens of the SAME window label; re-check inside `open_deferred`'s main-thread closure makes it airtight in code but untested. (4) `let _ = window.hide()` in `src-tauri/src/shell/mod.rs` silently drops hide failure on close-to-tray - inconsistent with `Existing::apply`, which now logs via `tracing::error!`. (5) `shell/tray.rs` `dispatch_tray_action` still uses `eprintln!` for region/settings/history failures while newer code paths use `tracing::error!` - unify. (6) TASK-025 no-key notice now shared via `src/components/ProviderKeyNotice.tsx`, but `RegionPreviewView` and `CaptionOverlayView` still hand-roll their own copies - dedup. (7) TASK-028 open item: right-aligned Badge/hotkey-chip/Button controls on home screen could not be pixel-verified in sandbox (suspected WebView2 repaint artifact under forced synthetic resize; not reproduced in headless Chromium) - awaiting owner's own visual check on release binary. | Open | 2026-07-11 |
| Live audio STT chậm trên CPU: whisper.cpp build với AVX2/AVX TẮT (whisper-rs-sys không set GGML_NATIVE/GGML_AVX2, ép BUILD_SHARED_LIBS=OFF nên không có runtime dispatch). Đo thật p95: tiny 6.2s, base 15.6s, small 57.4s - đều vượt ngân sách 3s | Feature `vulkan` (opt-in, tắt mặc định, PR #67) tăng tốc GPU: base p95 0.79s trên iGPU Intel Arc. Người cần live audio nhanh tự build `--features vulkan` (cần Vulkan SDK). Mặc định vẫn CPU để không phụ thuộc GPU | Open (opt-in workaround) | 2026-07-12 |
| Vulkan KHÔNG bật được mặc định: whisper.cpp đếm thiết bị Vulkan ngay khi load model (ggml-backend-reg static init, TRƯỚC use_gpu), không driver thì ném vk::SystemError vượt FFI -> abort cả process; binary vulkan còn phụ thuộc load-time vào vulkan-1.dll. Probe ash gate use_gpu KHÔNG cứu được vì crash ở device-enumeration của ggml, không phải use_gpu của ta | Giữ Vulkan opt-in (owner quyết định 2026-07-12). Bật mặc định cần fork/patch whisper-rs-sys (DELAYLOAD vulkan-1.dll + try/catch quanh ggml_vk_instance_init) - cấp ADR, để dành | Deferred | 2026-07-12 |
| Frontend-only change không được nhúng vào release binary trừ khi cargo relink: release ost.exe nhúng dist/ lúc compile Rust, nên đổi TS/CSS mà không đổi Rust thì tauri build tái dùng frontend cũ | Xoá src-tauri/target/release/ost.exe (hoặc touch một file Rust) trước khi build lại để ép re-embed | Open | 2026-07-12 |
| Live audio nhanh cần build opt-in Vulkan (owner chọn 2026-07-12): bản phân phối mặc định vẫn CPU (an toàn cho máy không GPU), người có GPU + driver Vulkan tự build bản GPU | Lệnh build (verify trên máy dev NVIDIA RTX 4070 + Intel Arc, Vulkan SDK 1.4.350.0): đặt VULKAN_SDK trỏ SDK, PATH có CMake + LLVM, rồi `npm run tauri build -- --no-bundle --features vulkan`. Binary link vulkan-1.dll, whisper chạy GPU (base p95 ~0.79s vs 15.6s CPU). CHỈ chạy trên máy có driver Vulkan - crash trên máy không driver (xem row ggml-vulkan static-init) nên KHÔNG bật mặc định | Open (opt-in) | 2026-07-12 |
